'use client';
import { Box, Typography } from '@mui/material';

export default function UserSettingsPage() {
  return (
    <Box>
      <Typography variant="h5" fontWeight="bold">
        Settings
      </Typography>
      <Typography color="text.secondary">Account settings will appear here.</Typography>
    </Box>
  );
}
