'use client';
import { Box, Typography } from '@mui/material';

export default function UserWishlistPage() {
  return (
    <Box>
      <Typography variant="h5" fontWeight="bold">
        Wishlist
      </Typography>
      <Typography color="text.secondary">Your wishlist items will appear here.</Typography>
    </Box>
  );
}
